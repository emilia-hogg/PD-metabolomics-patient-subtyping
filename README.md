# PD-metabolomics-patient-subtyping

All scripts use a shared configuration file that loads the file `restarted_pca_kmeans_working_session.RData` that is necessary for the code to run. A table (Appendix 2) outlining the pipeline architecture is included in the dissertation (not in this repository). 
