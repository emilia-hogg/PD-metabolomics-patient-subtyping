# PD-metabolomics-patient-subtyping

All scripts use the file `restarted_pca_kmeans_working_session.RData` as a shared configuration file and it is necessary for the code to run. A table (Appendix 2) outlining the pipeline architecture is included in the dissertation (not in this repository). 
